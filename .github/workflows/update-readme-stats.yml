name: Update Repository Index

on:
  push:
    branches:
      - main
      - master
    paths:
      - '**/*.yaml'
      - '**/*.yml'
      - 'README.md'
  workflow_dispatch:
  schedule:
    # Run daily at 00:00 UTC
    - cron: '0 0 * * *'

jobs:
  update-stats:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pyyaml

      - name: Validate templates
        run: python3 scripts/validate_templates.py

      - name: Update README with stats
        run: python3 scripts/update_readme_stats.py

      - name: Update templates.json index
        run: python3 scripts/update_templates_json.py

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add README.md templates.json
          git diff --staged --quiet || git commit -m "📊 Auto-update README stats and templates.json [skip ci]"
          git push
